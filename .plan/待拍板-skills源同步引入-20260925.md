---
type: approval
date: 2026-09-25
status: closed
origin: proactive
---

# 待拍板：skills 源（workdir/skills）同步引入

> **结算（2026-09-25）**：用户拍板原话「A.A1 B.B1 C.C1 D.D2」，四项全部裁决，落地票立入 `.plan/skills-source-sync/tickets/`（01↔A、02↔B、03↔C、04↔D）。本文档 status: closed，保留价值=四项差异的完整考证依据与 A1/B1/C1/D2 的执行口径（YAGNI 段原文、research 击破的 adapter 适配点、prototype 两块实质点边界）。

## 背景

用户要求对比 `packages/dsh-plan-view/skills/`（本仓，分发正本）与 `/Users/huzilin/workdir/skills/skills/`（改造源，Matt Pocock skills 仓库本地克隆，main 与 origin 同步）的 9 个同名 skill，确认源是否升级、有无可引入项。

两侧全量 diff + git 历史考证（2026-09-25 执行）后，**源侧在这 9 个同名 skill 的文件上自 2026-08-15~08-19 后零新提交**；差异由两部分构成：①本仓拷入的版本早于源侧 7~8 月的升级（落后）；②本仓侧大量有意改造（必须保留）。逐 skill 判定：

| skill | 判定 | 依据 |
|---|---|---|
| diagnosing-bugs | 无引入项 | 源 08-19 后无变动；本仓多 1 行 plan-lint 回扫 checkbox（09-25 eabc203 拷入+改造） |
| grill-me | 无引入项 | SKILL.md 逐字节一致；仅源侧多 `agents/`（见下「既有拍板」） |
| grill-with-docs | 无引入项 | 本仓 09-25 de5b4a7 自含化改造版更新（domain-modeling 职责内联、ADR's→ADRs） |
| research | 无实质引入项 | 本仓有 `.plan/research/` 落盘 + 环境兼容改造；源侧仅文风（em-dash 清除）与 description 微调 |
| to-tickets | 无实质引入项 | 本仓深度本地化（plan 协议挂接、票面词表、`.plan/` 契约、去 tracker）；frontier 概念本仓保留（SKILL.md:108,112 已改写接词表） |
| domain-modeling | **待拍板 A**：源侧有升级，本仓无明显改造 | 源侧 08-13 e12e7ec 重写 description 触发词、08-19 清 em-dash；本仓（08-28 8c9a91d 入库）停在 06-17~08-13 之间的旧版 |
| improve-codebase-architecture | **待拍板 B**：源侧有实质段落本仓缺失 | 源侧 07-13 45afd80 加「Scope before you scan: YAGNI」扫描定向段（commit 历史热点探测），本仓版无此段；本仓另有自含化改造（proceed silently ×2、subagent 环境兼容措辞）须保留 |
| wayfinder | **待拍板 C**：源侧有实质机制本仓缺失 | 源侧 07-13 2602257 加 research 票并发击破（charting 时对 research 票 fire subagents、resolving 时 research 票不限一票/ session）；本仓 adapter 化大改造版（TRACKER-MARKDOWN.md、Two rules、What stays prose、undermined 等）无此机制但保留了 Research 票类型（SKILL.md:80） |
| prototype | **待拍板 D**：两侧路线分叉，方向题 | 见下详述 |

**既有拍板（不重开）**：源侧每个 skill 下的 `agents/` 目录（2026-07-13 697d4ce 全仓加入，OpenAI/Codex harness 元数据，仅 display_name 两行）——09-25 eabc203 已拍板「不合本仓 harness-agnostic 定位故不拷」，本次维持不引入。

**范围外告知**：源仓库 9 月新动作均不在本仓名下同名范围——新增 `pr` skill（09-17~18，PR 描述模板）、retro 更新（09-15 deterministic-checks）、link-skills 停止链 misc/（09-03）；源仓库另有 1 处本地未提交改动（productivity/grilling/SKILL.md，grilling 不在本仓名下）。如需把 `pr` 作为新 skill 引入本仓，另立议题。

---

## 待拍板 A：domain-modeling 三文件整体对齐源侧新版

**是什么**：源侧（engineering/domain-modeling）比本仓新两步：①08-13 e12e7ec 把 description 触发词从「pin down domain terminology or a ubiquitous language…」重写为「discussing codebase terminology, writing or editing a CONTEXT.md, or recording or editing an ADR」；②08-19 全仓 em-dash 清除的文风统一。SKILL.md + ADR-FORMAT.md + CONTEXT-FORMAT.md 三文件同批。

**语境**：本仓版无本地改造痕迹（diff 全部为上述两步的反向残留）。plan 系 skill 未挂接此 skill（无词表/plan-lint 耦合点，已核实 diff 中无本仓协议痕迹）。

**选项**：
- A1（推荐）：三文件整体以源侧覆盖。风险最低——本仓无会丢失的本地改造；description 变化属 skill 触发面变化，改后需新开会话生效（既有惯例）。
- A2：不动。代价是持续落后源侧，且下次同步时 diff 噪音不变。

**性质标注**：「本仓无本地改造」为本次 diff+git 考核结论（已核实）；「需新开会话生效」为既有记忆惯例（文档已写明）。

**裁决（2026-09-25）**：拍板 **A.A1**——三文件整体以源侧覆盖。落地票：`.plan/skills-source-sync/tickets/01-domain-modeling-对齐源侧新版.md`。

---

## 待拍板 B：improve-codebase-architecture 手工移植 YAGNI 扫描定向段

**裁决（2026-09-25）**：拍板 **B.B1**——手工移植该段，保留本仓全部既有改造。落地票：`.plan/skills-source-sync/tickets/02-improve-codebase-architecture-移植YAGNI定向段.md`。

**是什么**：源侧 SKILL.md 在「读 glossary/ADR 之后、spawn subagent 走查之前」有一段本仓版缺失的扫描定向（07-13 45afd80 引入），原文照抄：

> **Scope before you scan: YAGNI.** Deepening a module pays off by making future changes to it easier, so put extra weight on the parts of the codebase that have recently changed. Decide *where* to look before you look:
> - If the user named a direction (a module, a subsystem, a pain point), take it, and skip the inference below.
> - Otherwise, walk back a good stretch of the commit history (`git log --oneline`) to find the codebase's hot spots, the files and areas that keep coming up, and let those paths pull your attention first. If the changes are scattered with no clear hot spot, widen the net.

**语境**：本仓版此位置直接是「Read the project's domain glossary … if they exist. Then walk the codebase — via a read-only exploration subagent …」。移植时插在这两步之间即可，与本仓自含化措辞（proceed silently、环境兼容）不冲突。

**选项**：
- B1（推荐）：手工移植该段（含小标题共 4 行），保留本仓全部既有改造。
- B2：整文件以源侧覆盖——**不可取**，会丢本仓 proceed silently 与 subagent 环境兼容改造（列出仅为完整对照）。

---

## 待拍板 C：wayfinder 手工移植 research 票并发击破机制

**裁决（2026-09-25）**：拍板 **C.C1**——移植，按 `.plan/research/` 落点与 adapter 语境两点适配。落地票：`.plan/skills-source-sync/tickets/03-wayfinder-移植research票并发击破.md`。

**是什么**：源侧 07-13 2602257（feat(wayfinder): burn research tickets down with subagents）给 wayfinder 加了 research 票的并发击破，本仓 adapter 化改造版基于更早版本，两处缺：①charting 步骤缺「对本轮新建的每个 research 票 fire 一个 subagent 调 research skill 并发解决，产物落在 throwaway `research/<name>` 分支并回链票据」；②resolving 约束「never resolve more than one ticket per session」缺「research 票例外」。

**语境**：本仓版保留了 Research 票类型（SKILL.md:80，AFK、调 research skill 出引证摘要），即票类型在、击破机制不在——研究票当前只能串行手解。移植需两处适配：①「branch 上留 context pointer」要按本仓 adapter 语境改写（本仓票据落 `.plan/`、无 issue tracker，产物落点建议沿用本仓 research skill 的 `.plan/research/<slug>.md` 惯例）；②「with the exception of research tickets」补回本仓 SKILL.md:105 一句。

**选项**：
- C1（推荐）：移植，按上述两点适配本仓 adapter 语境。
- C2：不移植。代价：Research 票类型有名无实（AFK 并发击破是它相对 Task 票的核心差异），且源侧后续演进若依赖此机制会越落越远。

**性质标注**：「本仓版基于更早版本」为 git 考证结论（已核实）；「产物落点建议 `.plan/research/`」为我的推断（待拍板确认）。

---

## 待拍板 D：prototype 路线拍板（两侧分叉，不能盲并）

**裁决（2026-09-25）**：拍板 **D.D2**——保留平台无关框架，手工移植 When-done primary source 捕获法与 LOGIC web 形态指引两块实质点。落地票：`.plan/skills-source-sync/tickets/04-prototype-移植上游实质点保留平台无关.md`。

**是什么**：本仓版 = 源仓库 2026-05-06 初版（TUI 终端应用路线）+ 一层**源仓库从未有过的「平台无关化」重写**（UI.md 主体改为三不变量 + TUI/desktop/games/CLI 翻译表，SKILL.md 运行示例改 make/cargo/mix）。源侧此后两次实质升级本仓均未跟上：①07-09~10 When-done 重构（原型作为 primary source 提交 throwaway 分支+在实现票留 context pointer，取代本仓版的 Delete or absorb/NOTES.md 捕获法）；②07-31 6bcbcb0 LOGIC 分支从 TUI 改为**单文件可分享 HTML demo**（无安装、可交给非开发者驱动、domain language 按钮）。

**语境与影响**：这不是文风差，是原型形态的产品方向题——源侧路线的收益是「非开发者（设计师/PM/领域专家）能亲手驱动状态模型」；本仓平台无关路线的收益是「宿主项目非 web 时也可用」。两侧各有一块对方没有的实质内容，整文件覆盖任一方都会丢一块。

**选项**：
- D1：跟上游——三文件整体以源侧覆盖（放弃平台无关化）。适合原型主要给非开发者看、宿主多为 web 项目的实际用法。
- D2（推荐）：保留平台无关框架，手工移植两块实质点——When-done 的 primary source 捕获法（ throwaway 分支+context pointer，替换 Delete or absorb 段）、LOGIC 形态说明中补「若宿主为 web，优先单 HTML 可分享 demo（非开发者可驱动）」作为 web 情形的形态指引。
- D3：不动，接受持续分叉。

**性质标注**：「本仓平台无关化是自定义改造」为 git 考核结论（UI.md 特征串源侧全历史无、已核实）；「本仓改造是有意为之还是早期拷入即冻结」无法从 git 判定（安装态不在 git），为**我的推断**——若当年是有意选 TUI/平台无关，D1 直接排除。

---

## 执行备注（拍板后）

- 落地类型：A/B/C/D 均为**代码/文档落地**（本仓 skill 文件修改，附 commit），非待立项。
- 改动后按惯例跑 `scripts/install-skills.sh` 同步安装态，并新开会话使 description 变化生效。
- 引入源侧文字时随带源侧文风（em-dash 已清），不与本仓 plan 系文风混排。
- 术语/触发面变更检查：A 项 description 重写属 skill 触发面变化，若引入，在 commit message 注明变更点。
