---
type: task
blocked_by: []
status: done
claimed_by: zcode
session: zcode-dsh-plugin-20260924
---

# 05: plan-lint 票形态门禁补两条（拍板「1」）

**What to build:** 2026-09-24 用户在收口会话拍「1」（方案 1）：plan-lint 补两条票形态检查，堵兜底网两个已识别的洞——①`tickets/` 目录下的文件无 frontmatter 或缺 `type`/`blocked_by` → 报；②合体 `tickets.md`（多票一文件会被视图当成一张票）→ 报。口径承接同日拍板 A：手写票不拦写入、格式必须同源 to-tickets，lint 兜底。

## Acceptance

- [x] 新增 [4] 票形态节：只查 `tickets/` 目录直属文件；合体 `tickets.md` 大小写不敏感；`type`/`blocked_by` 缺一即报
- [x] 合成自测：裸票 / 缺 blocked_by / 合体 tickets.md 三红，合规票绿
- [x] 实盘：dsh-plugin 29 个 markdown 全绿；novel 276 个 markdown **零 ticket-shape 发现**——82 张存量票（novel 78 + 本仓 4）本就合规，按形态变更回扫条款回扫量为零
- [x] 六个收口 skill 安装态同步（install 注入面 mp-plan-approve / mp-plan-sync / mp-diagnosing-bugs / run-qa-testcases / mp-implement / mp-implement-spec）

## 落地注

- 2026-09-24 实施并同步。检查逻辑：`case "$d" in */tickets)` 只匹配 tickets 目录直属文件，嵌套子目录不误伤；`fm_field` 逐票取 `type`/`blocked_by`，空值即报（`blocked_by: []` 合法——字段存在即可）。
- novel 侧 7 项存量发现（6 missing-map + R12 双档）与本票无关，归 novel 挂账-18。
