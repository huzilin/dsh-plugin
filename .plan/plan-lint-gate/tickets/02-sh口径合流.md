---
type: task
blocked_by: []
status: done
---

# 02: sh 口径合流——对齐 mjs 判定与豁免，双仓对拍绿

**What to build:** `scripts/plan-lint.sh`（收口后的唯一实现）与 `scripts/plan-lint.mjs` 判定口径一致：①跳过集合补 `qa`/`assets`/`node_modules`（落实 2026-09-20「plan-lint 先不管 qa：跳过集合保持不变」拍板）；②同票双档判定从「全局 basename」改为 mjs 的 effort 内判定口径；③`handoffs`/`.archive` 豁免保持。对拍正本是 mjs（它从未在 novel 误报）。

**Blocked by:** None — can start immediately

**Source spec:** `.plan/plan-lint-gate/待拍板-plan-lint随skill内置分发-20260923.md`（项 2A，原话「2A」）

## Acceptance

- [x] sh 版对 `novel/.plan` 不再报 qa 三件套（cases/test）与各 effort 自有票号（01-/02-…）的双档误报
- [x] 对 novel 与 dsh-plugin 两仓实跑，sh 与 mjs 的发现清单逐条一致（novel 11 条 = mjs 11 条，diff 为空；dsh-plugin 双方均零发现）
- [x] 对 dsh-plugin 本仓实跑仍绿（既有基线不回退）
- [x] mjs 侧 2026-09-20 handoffs 豁免与 ledger 行为在 sh 中不回退（遍历剪枝同款 SKIP 集合）

## 落地注

- 2026-09-23 实施完毕。sh 重写为镜像 mjs 三段语义：①遍历整棵剪枝（SKIP：.archive/node_modules/assets/qa/handoffs/ledger/隐藏项）；②双档按完整文件名分组（map/readme 豁免、`type: ledger` 是登记簿不是票）；③状态头按 type 分词表（approval 四字段+四态 / task 五态 / 「待拍板」裸文件兜底）。
- **额外根治一个潜伏 bug**：macOS `sort`/`uniq` 在 UTF-8 collation 下把不同汉字文件名判等（实测 en_US.UTF-8 将 150 行收成 148 行），旧 sh 的「同票双档」假阳性半数来源于此、半数来源于全局 basename 分组——已 `export LC_ALL=C` 按字节比较根治（脚本头有注）。
- 附带发现：novel `state-machine/改造工单/R12-契约更名` 与 `specs/R12-契约更名` 双档、5 个 effort 的 `spec.md` 同名双档、4 目录缺 map、5 张 task 票用词表外状态 `pending`——均为真发现，归 novel 侧收口，不在本仓处理。
