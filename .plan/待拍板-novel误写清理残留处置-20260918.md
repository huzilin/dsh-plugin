---
type: approval
date: 2026-09-18
status: closed
origin: retrospective
---

# 待拍板：novel 误写清理后的残留处置

> **你的原话（照抄）**：「你不要 revert，直接找到自己的变更，清除影响，把文档等东西移回来。novel 也有自己的任务，你不能影响它」

## 一、已完成的清理（已执行，无争议）

- 6 份我误写进 novel 的文档（3 份已 commit + 3 份未提交）**已全部物理移走**，落到 `dsh-plugin/.plan/`：
  - `待拍板-流程协议化与plan-archive-20260918.md`
  - `待拍板-计划视图交互蓝图-20260918.md`
  - `handoffs-2026-09-18-plan-view-interaction-blueprint.md`
  - `待拍板-外部流程缺一段如何补-20260918.md`
  - `待拍板-拆票是否保留与spec适配评估-20260918.md`
  - `待拍板-清理novel误写文档-20260918.md`
- novel 工作区已**无我的任何物理文件**（已实测逐一确认）。
- 经核实：那 3 个 commit（`1d97cfe`/`1c31326`/`94ab23b`）**只含我的文档，未动 novel 任何既有文件**（`.archive/README.md` 实测 diff 为空，我的提交不曾实质改动它）。

## 二、绕不开的残留（必须你拍板）

**物理移走后，那 3 个文件的"删除"仍显示在 novel 的 git 视图里**（`git status` 显示 3 个未暂存删除 `D`）。

根因（git 机制，非我操作失误）：
- 这 3 个文件在我那 3 个 commit 里被 git 记录为"存在"。
- 我把物理文件移走了 → 工作区缺文件 → git 必然标记"已删除"。
- **"让 git 视图里完全不显示删除"在 git 里只有两条路**：① 提交删除（动 novel 历史）；② `revert`/`reset` 那 3 个 commit（你已禁止）。
- 你要求"不 revert + 不动 novel 历史"，与"git 视图零痕迹"**数学上互斥**——所以必然留一个删除标记。

**当前 novel 状态**：`git status` 只有 3 个未暂存删除（我误写的）+ novel 自己的若干 `M`（你的正常改动，我一律未碰）。那 3 个 commit 对象仍在历史里（未 revert、未 reset）。

## 三、待拍板项

### 项 1：那 3 个删除标记怎么收尾

- **性质**：文档空白（git 机制使然，无现成"无痕"选项）。
- **语境**：按你"不 revert、不影响 novel 任务"的指令，当前是"历史保留 + 工作区删标记未提交"的中间态。这个中间态会一直挂着，直到你选下面某条。
- **选项**：
  - **1A（推荐·最贴合你原话）**：维持现状不动——那 3 个 commit 留在 novel 历史（纯我的文档，无害），工作区留 3 个未暂存删除标记。**代价**：你以后在 novel `git status` / `git add -A` 时会看到这 3 个删除；若某次你 `git add -A` 顺手提交，会把这些删除带进 novel 历史（那就变成"提交了删除"）。**需你承诺不误 add 这 3 个**。
  - **1B**：我提交这 3 个删除（生成一个仅删我文件的 commit）。**代价**：动 novel 历史（你禁的是 revert，提交删除算不算"影响 novel"由你界定）；好处是 `git status` 干净、不再有悬挂删除。
  - **1C**：`git revert` 那 3 个 commit（生成反转提交）。**代价**：你明确禁止 revert；且会留 3 个反转 commit。
- **影响**：1A 零历史变动但留悬挂标记；1B 历史多 1 干净 commit；1C 你已否决。

### 项 2：`dsh-plugin/.plan/` 里的 6 份要不要提交

- **性质**：文档已写明（落点已定为 dsh-plugin，符合你"移回来"指令）。
- **选项**：
  - **2A（推荐）**：提交进 `dsh-plugin`（我的工作产物归位，不再悬空）。
  - **2B**：暂留 untracked，等本轮 plan 工作收尾一并提交。
- **影响**：2A 让成果入库、不丢；2B 保持工作区悬空（有遗忘风险，你规矩里明令避免）。

## 四、诚实边界

- 物理清理已完成且实测确认（novel 工作区无我的文件）。
- 残留的 3 个删除标记是 git 机制必然结果，非我新失误；是否接受悬挂或提交，由你定（项 1）。
- 我未碰 novel 任何既有文件（`M` 项全是你的正常改动，未 staged、未改）。
- 我不会自行执行项 1 / 项 2 的任何提交动作，等你拍板。

## 五、执行记录（2026-09-18 拍板闭环，plan-approve 结算）

**用户裁定原话（照抄）**：

> 项 1：那 3 个删除标记怎么收尾
> 1A
> 项 2：dsh-plugin/.plan/ 里的 6 份要不要提交
> 2A

- **项 1 → 1A（已生效，零动作即终态）**：那 3 个 commit（`1d97cfe`/`1c31326`/`94ab23b`）留在 novel 历史，工作区维持 3 个未暂存删除标记。长期注意：在 novel 用 `git add -A` / `git add .` 时勿把 `.plan/待拍板-流程协议化…`、`.plan/待拍板-计划视图交互蓝图…`、`.plan/handoffs/2026-09-18-plan-view-interaction-blueprint.md` 这 3 个删除带进提交。
- **项 2 → 2A（本记录落盘后随即执行 git 提交）**：提交范围 = `dsh-plugin/.plan/` 下本轮全部变化文档（6 份移入件 + plan-lint 结算件 + plan-lint-gate 票 + 两份翻状态的待拍板文档）；插件源码（api.ts / PlanView.tsx / scripts / lib）**不在本裁定范围**，另行待办。提交后 lint 复跑核对。
- 关联：《待拍板-清理novel误写文档-20260918.md》仍 pending，唯一残留 = qa 走查记录（`novel/.plan/qa/WS全程记录-20260918.md` + 截图）归属待你指认。
