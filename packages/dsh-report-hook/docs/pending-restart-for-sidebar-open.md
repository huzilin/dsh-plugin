# 待拍板事项：让 sidebar_open 工具生效（需要重启 DSH）

> 本文是审批材料。四类信息齐全：原文照抄 + 该原文的语境 + 相关条款关系 + 选项与影响。
> 性质标注：**【文档已写明】/【文档有空白】/【我的推断】** 三类分开标注，不混在一张表里。

---

## 一、要拍板的是什么

**一个动作：重启 DSH 进程，让已改好的 `agentOpenTools: true` 被真正加载。**

背景一句话：你要的「文档直接 sidebar 打开」这条能力，配置我已经改好了，
但**它需要一次进程重启才会出现在我的工具集里**。重启这个动作牵涉到你本机的
桌面壳认证（下面的「风险」一节），所以我不擅自做，交给你定。

---

## 二、原文照抄（你的两句话）

**【文档已写明：本次会话用户原话】**

> 「选择 A，这样之前的 @packages/dsh-report-hook/ 嵌入到了汇报环节，这里可以直接写到这个里面。」

> 「里面要求将要拍板并落的文档，组后直接调 sidebar_open 打开。并要求一般的文档输出格式，要求都是 sidebar 打开的。」

（注：第二句里「组后」应为「最后」的输入笔误，我按「最后」理解并落地。
若你的原意不是「最后 / 最终」，请纠正我。）

### 这两句话在讨论什么（语境）

上一轮我回答了你的问题：「有没有办法让 dsh 返回的内容点击后直接在 sidebar 打开？」
我的结论是：这件事 DSH 生态里**已经有现成机制**，分两层——

1. **链接层**：我在回复里写的 Markdown 链接，`dsh-better-sidebar` 插件会在页面最外层
   拦截点击，改为在右侧边栏打开（http 默认接管、https 默认不接管）。
2. **主动推送层**：插件还给模型提供了一个叫 `sidebar_open` 的工具，我能主动把
   文件 / 文件夹 / 网页推进侧边栏打开——**但它默认是关的**（`agentOpenTools` 默认 `false`）。

我当时给了你三个选项，你选了 **A（开启这个开关）**，并追加要求：
把「文档一律用 sidebar 打开」这条规则**写进 report-hook 的汇报环节**。

### 相关条款关系（现有规范里的位置）

`dsh-report-hook` 是「你每次发言时，判断你是不是在要决策 / 审批，命中就往我上下文里
注入一段汇报规范提醒」的钩子。它注入的提醒原本有 ①~④ 四条：

| 条款 | 内容 | 约束力 |
|---|---|---|
| ① | 说听得懂的话——少用英文与非技术专业词 | 命中即适用 |
| ② | 展开说，不许甩词条 | 命中即适用 |
| ③ | 要审批必须交文档（含原文照抄 + 语境 + 条款关系 + 选项与影响） | 需要拍板时 |
| ④ | 性质必须分类标注（已写明 / 有空白 / 推断） | 需要拍板时 |

**你这次的要求 = 新增第 ⑤ 条，且它有一条特殊的约束力：**

- ⑤ 管的是**交付形态**（文档要打开，不是只给路径），不是文案内容；
- **⑤ 与「是否要拍板」无关**——这是关键。因为 report-hook 只在命中「要决策/审批」时
  才注入，如果 ⑤ 依附于拍板判定，那么**一般的文档输出（日报、报告、分析）就不会被覆盖**，
  违背你「一般的文档输出格式，要求都是 sidebar 打开的」这句明确要求。
  所以 ⑤ 在文案结语里单独声明了「与是否要拍板无关」。

---

## 三、我已经做完的部分（无需你决策，仅告知）

| 事项 | 状态 | 性质 |
|---|---|---|
| `~/.dsh/settings.yaml` 加 `dsh-better-sidebar.agentOpenTools: true` | 已改，YAML 校验通过，旧文件已备份为 `settings.yaml.bak-pre-agentopentools-20260911194643` | 【已落盘】 |
| report-hook 注入文案新增第 ⑤ 条 | 已改，13/13 单测通过，端到端注入实测通过（注入文本 764 字符、①~⑤ 五条齐全） | 【已落盘】 |
| report-hook 新增 `README.md`（记录 ⑤ 的来由与前置依赖） | 已写 | 【已落盘】 |
| `dsh-zvec-router` 删除 | 见第四节 | 【已落盘】 |

端到端实测（非推断，是真实执行结果）：

```
$ echo '{"prompt":"这个方案要不要拍板？"}' | node scripts/report-hook.mjs
→ 输出注入 JSON，additionalContext 含 ⑤ 且点名 sidebar_open ✅
$ echo '{"prompt":"你好"}' | node scripts/report-hook.mjs
→ 无输出（不注入）✅
```

---

## 四、附带完成：dsh-zvec-router 的删除（你要求的第二件事）

**你原话：**【文档已写明】「另外 dsh-zvec-router 可以删掉了。」

我做了**影响面核查**，结果与「只删一个目录」不同，有两处必须一并处理的残留：

### 已清理

| 位置 | 做了什么 | 为什么 |
|---|---|---|
| `packages/dsh-zvec-router/`（3 个文件） | `git rm` 删除 | 你要求删的包本体 |
| `~/.dsh/.agent-presets/full/agent.cordis.yml` 末尾 9 行悬空注释 | 删除 | 该注释描述的是已不存在的 zvec-router 挂载行，挂载条目本身此前已被移除，只剩注释 |
| 同上，`tool-fs-search` 的 `promptGuidance` | **由 `false` 改回 `true`** | **这是关键**：它原本被设为 `false`，唯一原因就是「给 zvec-router 的路由版 `tool:grep`/`tool:glob` 腾段名」（同一 preset scope 内段名唯一，官方段不关会撞名导致 preset 挂载失败）。router 删了若不改回，grep/glob 会**没有任何使用引导**——这是删除动作的隐性后果 |

### 核查后确认「无需动」的（说明为什么）

| 位置 | 结论 | 依据 |
|---|---|---|
| profile `package.json` / `cordis.patch.yml` / `pnpm-workspace.yaml` | 已无 zvec 引用 | 你在 9-10 21:13 已先自行摘除（留有 `.bak-zvec-20260910211330` 备份） |
| profile `node_modules/dsh-zvec-router` | 已不存在 | 同上 |
| profile `pnpm-lock.yaml` | 无 zvec 条目 | grep 零命中 |
| dsh-plugin 仓库其它文件 | 无 zvec 引用 | 全仓 grep 零命中 |

### 我**没有**动的东西（重要）

`~/.dsh/profiles/web/` 下三个备份文件 `*.bak-zvec-2026091*`，**我没删**。原因：
它们不只是 zvec 的差异，还包含更早的插件状态（`dsh-prompt-enhancer`、`dsh-approve`、
`meow-smooth` 等已在当前配置里发生变化）。而且 `~/.dsh` **不是 git 仓库**，
删掉就没有第二份了。留着无害，删了丢历史。若你要清，告诉我。

---

## 五、需要你拍板的：重启

### 为什么非重启不可（【我的推断】，附证据）

- 当前 DSH 进程：PID `4994`，**启动时间 9月11日 11:35:50**；
- 我改 `settings.yaml` 的时间：**19:46:49**；
- 进程启动**早于**配置修改 8 小时。

插件的开关逻辑是挂在自己的 settings 服务上、监听 **settings commit 事件**的
（源码 `dsh-better-sidebar/src/index.ts:797-827`：`scope.watch(() => syncOpenToolsGate())`）。
我在**文件层面**改了 YAML，但运行中的进程并没有收到一次 commit 事件，
所以这个开关目前**不会**在运行进程里生效。→ **结论：需要重启。**
（这条是推断 + 源码证据，不是文档明文写的，我如实标注。）

### 重启的风险（这条是实打实的坑，必须你知道）

**【文档已写明：你的既有记忆】** `viking://user/default/memories/preferences/huzilin/dsh_web_auth.md` 记载：

> 生产 web 由手工命令行拉起，`DSH_WEB_NO_AUTH=1` 只存在于当时进程 env，
> 不在 dsh-services 注册表也不在任何 .env/settings——**任何重启必须显式带
> `DSH_WEB_NO_AUTH=1` 前缀**，否则桌面壳裸 URL 401。

我已实测确认这条仍然成立——刚才查了运行进程的环境变量：

```
$ ps -Eww -p 4994 | grep DSH_WEB_NO_AUTH
DSH_WEB_NO_AUTH=1        ← 只在这个进程的内存里，磁盘上任何配置文件都没有
```

所以：**如果我直接重启而不带这个变量，你的桌面壳（DeepSeek-Harness.app）会 401 打不开。**

当前进程完整启动命令（重启必须还原它）：

```
/opt/homebrew/Cellar/node/26.8.1/bin/node --import tsx/esm \
  /Users/huzilin/workdir/deepseek-harness/apps/cli/src/bin.ts web \
  --host 0.0.0.0 --no-open --trusted-host=s-mac-mini.tail3b3293.ts.net
```

### 选项

| 选项 | 怎么做 | 影响 | 代价 |
|---|---|---|---|
| **A. 我来重启（推荐）** | 我用 `DSH_WEB_NO_AUTH=1` + 上面那条完整命令重启，重启后验证侧边栏与工具是否生效 | 开关生效，我下轮就能主动推文档进侧边栏 | 当前网页会话短暂断开，网页会自动重连；正在跑的后台任务会中断 |
| **B. 你自己重启** | 你手动执行带 `DSH_WEB_NO_AUTH=1` 的命令 | 同上 | 你需要复制粘贴命令 |
| **C. 先不重启** | 配置留着，等你下次自然重启（比如升级 DSH 时） | ⑤ 条款已生效（它只是文案），但 `sidebar_open` 工具**暂不可用**，我会退化到「给路径」 | 你要求的「直接 sidebar 打开」体验暂时拿不到 |

**我的建议：选 A。** 理由：⑤ 条款要我打开文档，但工具没生效我就只能给路径——
等于你要的效果差最后一步；而重启的风险点（`DSH_WEB_NO_AUTH`）我已经查清并有应对。

### 需要你回答

1. 是否同意重启？选 A / B / C。
2. 若同意重启，**现在**是否有正在跑的重要后台任务需要先等它结束？

---

## 六、附：⑤ 条款落地后的实际效果（供你判断是否是你想要的）

开启后，我产出文档时的行为会变成：

```
写盘 → sidebar_open(绝对路径) → 侧边栏编辑器直接打开该文档全文
```

覆盖范围（按你原话的两层）：

1. **要拍板的审批文档**（如本文）→ 落盘后立即打开；
2. **一般文档输出**（报告 / 设计稿 / 分析 / 总结 / 方案对比……凡落盘成文件）→ 同样一律打开。

退化：工具不可用时退化为「给路径」并说明原因。

**一个我要提前说清的边界**【我的推断】：`sidebar_open` 的打开目标是
「**本地文件 / 本地文件夹 / http(s) 网页**」三类。它**不能**打开
「我回复正文里的某一段文字」——正文段落没有文件实体可打开。
所以「文档类产出」能一键打开，「聊天正文」仍然是读屏。
如果你期望的是后者，那是另一个需求，需要单独做，请告诉我。
