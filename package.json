{
  "name": "dsh-plugin",
  "private": true,
  "description": "DeepSeek Harness (DSH) plugin monorepo · 多插件开发与托管仓库",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "test": "pnpm -r test",
    "build:client": "pnpm -r --if-present build:client"
  }
}